[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
extend-select = ["I", "UP", "B", "BLE", "SIM", "C4", "RUF", "TID252"]

[tool.ruff.lint.isort]
known-first-party = ["models", "sources"]

[tool.ruff.lint.flake8-tidy-imports]
ban-relative-imports = "all"